<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- As in chiseled, Alpine and Native AOT images. Run with -p:InvariantGlobalization=false to compare. -->
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>
</Project>
